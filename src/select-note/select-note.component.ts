import { Component } from '@angular/core';

/**
 * This is basically an empty component for when we don't have a note selected.
 */
@Component({
  selector: 'select-note',
  template: `select a note`,
  styles: [
  ]
})
export class SelectNoteComponent {
  constructor () {}
}